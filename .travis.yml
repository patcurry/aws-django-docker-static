# aws-django-docker-static/.travis.yml

language: node_js

node_js:
  - "8"

install:
  - "npm install"

script:
  - "npm run test"
  - "npm run build"

before_deploy: "echo 'How do I deploy only the correct files?'"

#deploy:
#  provider: s3
#  access_key_id: "YOUR AWS ACCESS KEY"
#  secret_access_key: "YOUR AWS SECRET KEY"
#  bucket: "S3 Bucket"
#  skip_cleanup: true
#  local_dir: build